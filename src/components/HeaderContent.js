import styles from './HeaderContent.module.css';

const HeaderContext = () => {
    return (
        <>
            <h1 className={styles.greetings}>
                <span>I`M</span> NIKOLAI
                <br />
                WEB <span>DEVELOPER</span>
            </h1>
            <p className={styles.explanation}>Helping you and your busines to grow up</p>
            <div className={styles['header-context']}>
                <p>
                    .header-context p import styles from './HeaderContent ,module.css'; [dataState, setDataState] = useState;(false);
                    users.map((user)=> user.id === key && setDataState(true))
                </p>
            </div>
        </>
    );
};

export default HeaderContext;
